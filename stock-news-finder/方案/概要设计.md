# 股票新闻分析系统 · 概要设计

> 版本：v1.3（2026-08-25，对应需求文档 v0.6）
> 路径约定：脚本均位于 `scripts/`（本文模块名省略此前缀，如 fetch_news.py 即 scripts/fetch_news.py）
> 定位：新闻驱动的选股研究漏斗（非交易系统）

## 1. 系统概览

```
                    ┌──────────────────────────────────────────┐
                    │              数据与配置（可热替换）          │
                    │  sources.json  companies.txt  products.txt │
                    └──────┬───────────┬───────────┬───────────┘
                           │           │           │
   ┌───────────────────────▼───┐   ┌───▼────┐  ┌───▼─────────┐
   │ 采集层 fetch_news.py       │   │关联词典 │  │产品词→板块   │
   │ loader 动态加载插件        │   │构建     │  │成分股解析    │
   │  ├ eastmoney_fast.py     │   │build_  │  │product_     │
   │  ├ wallstreetcn_live.py  │   │dict.py │  │lookup.py    │
   │  ├ eastmoney_search.py   │   └───┬────┘  └───┬─────────┘
   │  ├ http_json.py (模板)   │       │           │
   │  └ file.py (模板)        │       │           │
   └───────┬───────────────────┘       │           │
           │ raw_news.json             │  stock_dict.json
           │ + raw/yyyymmdd.json       │  + board_cache.json
   ┌───────▼───────────────────────────▼───────────▼─────────┐
   │ 解析+关联+信号 match_score.py                            │
   │  ├ 三通道命中: 文本匹配(1.0) / 官方标注(1.2) / 产品传导(0.6)│
   │  ├ LLM 事件分类 (llm_classify.py, glm-4v-flash, 可降级)  │
   │  └ 打分: 影响分 × 置信度 × 命中强度 × 时效衰减(3天半衰期)  │
   └───────┬─────────────────────────────────────────────────┘
           │
   ┌───────▼──────────────┐   ┌──────────────────────────┐
   │ 展示: daily/yyyymmdd.md│   │ 验证层(未建): 回测/胜率    │
   │ + signal_report.md    │   │ 需 raw/ + daily/ 历史积累 │
   │ + 微信推送(push_daily) │   └──────────────────────────┘
   └──────────────────────┘
```

## 2. 模块设计

### 2.1 采集层（插件化）

**设计原则**：一源一模块，接口统一，动态加载。新增源零侵入。

| 组件 | 职责 |
|---|---|
| `fetchers/SPEC.md` | 插件接口规范：`fetch(cfg)→[news]`（6 固定字段）+ `selftest()→bool` |
| `fetchers/<type>.py` | 各源采集器：东财快讯（游标翻页/全天/回补/stockList）、华尔街见闻（含回补）、**东财关键词搜索（宏观/地缘定向，三层过滤）**、http_json/file 通用模板 |
| `fetch_news.py` | loader：读 sources.json → 动态 import → 汇总去重 → 输出 + 按日归档 |
| `sources.json` | 源配置（enabled 开关、私有参数） |

**数据流**：`fetch(cfg)` → 全量当日新闻（`_stop_date` 注入日期边界）→ 标题去重 → `output/raw_news.json`（当日工作区）+ `raw/yyyymmdd.json`（历史归档，增量合并，以旧归档为去重基准）

**新闻记录契约**（全系统统一）：

```json
{"title": "...", "body": "...", "time": "YYYY-MM-DD HH:MM:SS",
 "source": "<type>", "url": "...", "stocks": ["601827"]}
```

### 2.2 关联层（三通道命中）

| 通道 | 权重 | 数据源 | 说明 |
|---|---|---|---|
| 文本匹配 | 1.0 | companies.txt（4500 只全 A 股 + 别名） | 名称+别名子串匹配，长词优先 |
| 官方标注 | 1.2 | 东财 stockList 字段 | 无歧义、权威；文本漏配时的兜底（每日独有 3~5 只） |
| 产品传导 | 0.6 | products.txt → 板块成分股 | 产品词命中新闻 → 关联板块内词典股票 |

词典维护四入口：手动编辑 / `import_excel.py`（Excel）/ `import_sz.py`（深市全量）/ `lookup.py --add`（单只查询追加）。

### 2.3 解析层（LLM 事件分类）

- 模型：glm-4v-flash（免费），批量 8 条/请求，只送预命中新闻（约 1/7）
- 输出：9 类事件枚举 + 影响分(-5~5) + 置信度(0~1) + 一句话理由
- 降级：无 key / 调用失败 → 自动回退正则规则，主流程不中断
- key 来源：环境变量 `GLM_VISION_API_KEY`（本机 ~/.zshrc）

### 2.4 信号层

```
信号分 = LLM影响分 × 置信度 × 命中强度权重 × 时效衰减(半衰期72h)
综合分 = 该股当日全部信号分之和
```

权重体系：官方标注 1.2 > 文本命中 1.0 > 产品传导 0.6（板块整体 0.4 预留未用）。

### 2.5 展示层

`daily/yyyymmdd.md`（Markdown，概览表 + 每股明细）：

- 概览：排名/公司/代码/综合分/新闻来源/正负面/事件
- 明细：LLM 理由、时间/事件/标题（超链接原文）
- 宏观/地缘动态独立板块：搜索源新闻按时间倒序，带主题标签/媒体名，不进主排名（D15）
- 同日多次运行覆盖，最新为准
- **微信推送**（scripts/push_daily.py）：Server酱·Turbo，key=SERVERCHAN_SENDKEY（~/.zshrc），
  免费额度 5 条/天；超 30KB 自动截断（保留概览表+宏观板块）；并入 run.sh 第 5 步，
  失败不阻断（日报始终在 daily/）

## 3. 关键设计决策（与需求文档 D1~D13 对应）

| 决策 | 一句话理由 |
|---|---|
| 配置文件驱动一切（源/词典/产品词） | 用户硬约束：动态添加、手动维护、热替换 |
| 插件化采集 + SPEC 规范 | 新源零侵入；subagent 可并行开发；selftest 强制自测 |
| LLM 只做分类不做匹配 | 匹配是确定性工作（词典足够），LLM 用在语义判断（分类/影响分） |
| 三通道权重阶梯 | 确定性递减：官方标注 > 文本 > 传导 |
| LLM 降级链 | 定时无人值守时代码保障可用性；手动模式同样受益 |
| 归档以旧库为基准去重 | 多源/多次运行增量合并，历史永不丢失 |
| 手动触发 | 用户决策：灵活性 + 当场可见；回补弥补历史缺口 |

## 4. 数据资产与目录

| 路径 | 内容 | 生命周期 |
|---|---|---|
| `sources.json` | 采集源配置 | 手动维护 |
| `data/companies.txt` | 全 A 股词典 4500 只 | 手动+导入脚本 |
| `data/products.txt` | 产品词表 | 手动 |
| `data/macro_keywords.txt` | 宏观/地缘关键词表（[search] 主词 + [expand] 分组扩展词） | 手动 |
| `data/board_cache.json` | 板块成分当日缓存 | 自动，按日失效 |
| `output/raw_news.json` | 当日全量新闻 | 每次覆盖 |
| `output/llm_events.json` | LLM 分类结果 | 每次覆盖 |
| `raw/yyyymmdd.json` | 新闻历史归档 | 永久（回测底稿） |
| `daily/yyyymmdd.md` | 信号日报归档 | 永久（回测对照） |

## 5. 外部接口清单

| 接口 | 用途 | 已知坑 |
|---|---|---|
| 东财 np-listapi 快讯 | 新闻采集 | 必须 req_trace 参数；sortEnd 游标翻页；可回翻约 4 交易日 |
| 东财 searchapi suggest | 公司名/板块代码查询 | 须过滤 Classify==AStock / BK |
| 东财 push2 clist | 板块成分股 | CDN 不稳：curl 子进程+多节点+代理/直连全排列重试 |
| 东财 datacenter F10 | 公司名录（深市导入用） | filter 参数被拒，全量拉取本地过滤 |
| 华尔街见闻 lives | 宏观快讯 | title 可空、秒级时间戳 |
| 东财 search-api-web | 关键词全文搜索 | jsonp 需剥壳；模糊匹配噪音大——标题必须含主题词过滤；词表 data/macro_keywords.txt |
| GLM chat completions | 事件分类 | key 在登录 shell 环境 |
| Server酱 sctapi | 日报微信推送 | 免费版 5 条/天；单消息约 32KB 上限；推送目标为个人微信服务号消息 |

## 6. 扩展点（对应需求 M3~M5）

1. **新采集源**：写 `fetchers/<type>.py`（按 SPEC.md）+ sources.json 一行；`--selftest` 体检
2. **LLM 动态词典**：llm_classify 输出词典外实体 → lookup.py 实时查代码（F3.3）
3. **回测验证层**：解析 daily/*.md + raw/*.json → 关联行情接口 → 分事件统计胜率（F7.x）
4. **产品词批量扩充**：从东财概念板块清单生成 products.txt 初版（F2.7）

## 7. 风险与演进

- 词典歧义（"机器人"类通用词公司名）是当前最大假阳性来源，演进方向：上下文校验/降权/白名单
- 时效衰减半衰期（72h）与事件权重待回测数据校准（R4/F4.3）
- 历史回补 raw ≠ 当时信号视角（R8），回测结论需打折或引入规则版本号
