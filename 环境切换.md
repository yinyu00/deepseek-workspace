# 环境切换指南（新 Mac 恢复 DSH 工作环境）

> 用途：换新机 / 重装系统后，从 GitHub 仓库恢复完整的 DSH 开发环境。
> 仓库：git@github.com:yinyu00/deepseek-workspace.git（Private）
> 最后更新：2026-09-12

## 已同步到仓库的内容

| 内容 | 位置 | 说明 |
|---|---|---|
| 模型配置 | `dsh-config/settings.yaml` | provider、模型白名单、默认模型、reasoningEffort 等 |
| Skills | `.dsh/skills/` | dsh-width-95p、glm4v-image-compress、user-profile 等 |
| 项目与素材 | `homm3/`、`stock-news-finder/`、各 md/pdf | workspace 全部工作内容 |

**不在仓库里、必须手动恢复的**：API Key（安全原因，绝不进 git）、插件、全局 shell 配置。

## 恢复步骤

### 1. 安装基础环境

```bash
# 安装 Homebrew（如无）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 pnpm
brew install pnpm
```

### 2. 配置 GitHub SSH 认证

```bash
ssh-keygen -t ed25519 -C "新机器标识" -f ~/.ssh/id_ed25519 -N ""
cat ~/.ssh/id_ed25519.pub
# 复制输出，添加到 https://github.com/settings/ssh/new
```

### 3. 克隆仓库（放到与旧机相同的路径，或任意路径）

```bash
git clone git@github.com:yinyu00/deepseek-workspace.git ~/ai/deepseek/harness/workspace
```

### 4. 接上 dsh 配置（软链）

```bash
mkdir -p ~/.dsh
ln -s ~/ai/deepseek/harness/workspace/dsh-config/settings.yaml ~/.dsh/settings.yaml
```

> 注意：如果 `~/.dsh/settings.yaml` 已存在（dsh 首次启动会生成默认配置），先删除或改名备份再建软链。

### 5. 填入 API Key（唯一不能从 git 恢复的部分）

方式 A（推荐）：启动 dsh 后在 **Models 页面**录入：

- `ZAI_CODING_CN_API_KEY`（GLM Coding Plan，glm-5.3 / glm-5.3-flash）
- `DEEPSEEK_API_KEY`（DeepSeek 官方按量，deepseek-flash）

方式 B：从旧机直接拷贝凭据文件（更快，但注意保密）：

```bash
scp 旧机:~/.dsh/.credentials.yaml ~/.dsh/.credentials.yaml
chmod 600 ~/.dsh/.credentials.yaml
```

### 6. 启动 dsh

```bash
cd ~/ai/deepseek/harness/workspace
pnpx @deepseek-ai/dsh web
# 打开 http://127.0.0.1:3080
```

### 7. 重装插件（可选）

插件不在同步范围内，按需重装：

```bash
dsh plugin add dshmarket
```

> 兼容性提醒：安装前确认插件版本与当前 dsh 版本兼容（参考 npm 发布日期）。
> 界面宽度等 UI 调整如需复刻，重跑对应 skill（如 dsh-width-95p）。

## 验证清单

- [ ] `cat ~/.dsh/settings.yaml` 能读到模型配置（软链生效）
- [ ] 模型下拉有 glm-5.3 / glm-5.3-flash / deepseek-flash
- [ ] 随便发一条消息，正常回复（Key 生效）
- [ ] `git status` 在 workspace 下正常工作

## 常见问题

| 问题 | 处理 |
|---|---|
| 报 MISSING_CREDENTIAL / no API key | 第 5 步没做，去 Models 页面填 Key |
| 软链后 dsh 不读配置 | 确认 `~/.dsh/settings.yaml` 是软链而非旧文件；重启 dsh |
| git push 权限错误 | 新机 SSH key 没加到 GitHub，回看第 2 步 |
| 旧机 GLM Key 失效 | launchctl/zshrc 里的旧环境变量方式与凭据库不冲突，以凭据库为准 |

## 日常同步（旧机上的习惯）

改动 workspace 内容或 dsh 配置后，说一句「提交同步」给 DSH 助手，或手动：

```bash
cd ~/ai/deepseek/harness/workspace
git add -A && git commit -m "sync: $(date +%F)" && git push
```
